import { SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN } from '@sentry/core';

/** Adds an origin to an OTEL Span. */
function addOriginToSpan(span, origin) {
  span.setAttribute(SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN, origin);
}

export { addOriginToSpan };//# sourceMappingURL=https://cursor-sourcemaps.s3.amazonaws.com/sourcemaps/1649e229afdef8fd1d18ea173f063563f1e722e0/node_modules/@sentry/node/build/esm/utils/addOriginToSpan.js.map
